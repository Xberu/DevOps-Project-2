apiVersion: v1
kind: Pod
metadata:
  name: aws-cli-pod
  namespace: default

spec:
  serviceAccountName: aws-cli-sa
  containers:
  - name: aws-cli-container
    image: amazon/awscli
    command: ["sleep", "infinity"]